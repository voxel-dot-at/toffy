# toffy

Toffy helps with creating ToF-camera based applicatoins. It is an overlay-framework on top of 
OpenCV (https://opencv.org/) and optionally PCL (http://pointclouds.org/) and makes heavy use
of Boost (http://www.boost.org/) shared pointers to re-use memory structures.
